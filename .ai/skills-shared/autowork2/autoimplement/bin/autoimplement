#!/usr/bin/env ruby
# frozen_string_literal: true

ENV['AUTOWORK_DB_PATH'] ||= File.expand_path(
  '../../db/autoimplement-development.db',
  __dir__
)

require_relative '../../config/boot'

AutoimplementCli.call(cli_args: ARGV)
