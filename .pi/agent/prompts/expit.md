---
description: Explain a subject using ordered example approaches
argument-hint: "[subject]"
---

Read and follow `~/.ai/skills-shared/expit/SKILL.md` immediately as an explicit `/expit` invocation.

Treat `$ARGUMENTS` as the invocation arguments. If it is empty, the invocation has no subject argument and the skill must resolve the subject from the preceding conversation.
